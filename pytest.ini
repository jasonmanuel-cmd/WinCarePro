[pytest]
addopts = -v --tb=short -p no:warnings -m "not e2e"
testpaths = tests
markers =
    smoke: Quick smoke test for main window
    e2e: End-to-end UI test requiring WinCarePro.Desktop.exe (Windows only)
    flaky: Test known to be unstable, needs retry
