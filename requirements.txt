# WinCare Pro — runtime dependencies (Python 3.11+ on Windows 11)
# customtkinter is PINNED below 6.0: the app is built and tested against the
# 5.2.x API line. Major CTk releases have broken apps before (see GH #764).
customtkinter==5.2.2
psutil==5.9.8

# --- Build-time only (create the .exe, not needed to run from source) ---
pyinstaller==6.21.0

# --- E2E testing (pywinauto + UIA for WPF) ---
pywinauto==0.6.9
Pillow==12.1.0
pytest-html==4.1.1
pytest-timeout==2.3.1
